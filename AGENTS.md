# Agent Guide

When reporting information to me, be extremely concise and sacrifice grammar for the sake of concision.

Always use the `discordgo` library for Discord API access instead of creating your own. Documentation for the library is at https://pkg.go.dev/github.com/bwmarrin/discordgo

Always use `gofmt -w` to format Go files before committing to git.
