apiVersion: v1
kind: Secret
# kubectl create secret generic {secret_name} --from-file={file_path} \
#   --dry-run=client -o yaml > {secret_yaml_file}
metadata:
  name: discord-wordle-bot-secret
type: Opaque
stringData:
  # Replace values below with your real `config.json` contents.
  config.json: |
    {
      "bot_token": "REPLACE_ME",
      "channel_id": "REPLACE_ME",
      "starter_prompt": "Hey, don't forget Wordle!",
      "tracked_user_ids": [],
      "timezone": "UTC"
    }
